services:
  - type: web
    name: currency-strength-widget
    env: python
    buildCommand: pip install -r backend/requirements_railway.txt
    startCommand: uvicorn backend.api_server:app --host 0.0.0.0 --port $PORT
    plan: free
